# 夜局优化计划

按优先级推进，已完成的内容以当前源码为准。

## 已完成

- **迁移修复**：网页服务从 `src` 读取资源；配置式开局补齐随机函数及校验；增加 HTTP 回归检查。
- **P0 操作**：加注框、滑杆和快捷金额同步；非法金额就近提示并禁止提交；全下、免费弃牌有二次确认；纪事浏览旧记录时保留滚动位置，提供“回到最新”。
- **P0 布局**：短窗口允许纵向滚动，操作区保持可见；压缩顶端人物，给底池与公共牌留空间；保留纯文字模式和减少动画偏好。
- **P1 存档**：每手结算后原子写入本地存储，保存筹码、庄位、手数和界面偏好；可继续或新开，校验损坏存档并安全回退。进行中的一手从最近已结算状态恢复。
- **P1 人物**：阿杰六状态透明精灵图接入，动作短暂显示后恢复平静；桌边聊天使用独立随机数，不再改变洗牌与电脑决策序列。
- **P2 初版策略**：电脑结合起手牌、牌型、位置、底池价格、风格和少量诈唬做启发式决策；对手暗牌不参与判断，固定场景检查不同性格。
- **P2 初版回顾**：本手显示净输赢、退回款和摊牌赢家最佳五张；本次打开期间可查看最近十手的文字事件与公开牌，弃牌暗牌不泄露。

## 下一步：按优先级

1. **P0 桌面验收**：在 Windows 125% / 150% 缩放和不同窗口尺寸下，核对拖动、最小化、最大化、关闭/重开，以及十手连续试玩与控制台错误。
2. **P1 文字可信度**：让文案进一步结合角色历史和公开下注，减少重复；让动作与牌力保持弱相关，避免形成“看表情猜牌”的捷径。确认阿杰图集节奏后再扩展其他四人。
3. **P2 完整复盘**：记录每轮起始筹码、行动前后下注与边池形成过程；回顾可逐步查看，不只列文字纪事。若有跨重启回顾需求，再持久化最近十手。
4. **P2 策略进阶**：加入筹码深度和对手近期行为；用更多固定局面观察策略质量，再决定是否引入胜率采样。保持本地、非阻塞。

联网对战、账号、3D 和大模型实时接管策略仍暂缓。大模型若用于文案，应有本地回退，且不读取其他玩家暗牌或决定筹码结算。
